const initialState = {
  projects: [],
  isLoggedIn: false,
  isLoading: false,
  errors: ''
}

const reducer = (state = initialState, action) => {
  switch(action.type) {
    default :
      return state
  }
}

export default reducer